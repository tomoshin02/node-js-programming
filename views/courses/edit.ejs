<h1>Let's create your Original Course</h1>
<form action="<%=`/courses/${course._id}/update?_method=PUT`%>" method="post">
  <p>What cooking recipe do you offer?
    <label for="inputTitle">Title:</label>
    <input type="text" name="title" id="inputTitle" placeholder="Course title" value="<%= course.title%>" autofocus>
  </p>
  <p>Please write your course flow
    <label for="inputDescription">Description:</label>
    <textarea type="text" name="description" id="inputDescription" value="<%= course.description %>" cols="30" rows="10"></textarea>
  </p>
  <p>
  Where are you going to hold this?
  <label for="inputZipCode">Zip-Code:</label>
  <input type="text" name="zipCode" id="inputZipCode" placeholder="Zipcode" value="<%= course.zipCode %>">
  <label for="inputAdress">Adress:</label> 
  <input type="text" name="adress" id="inputAdress" placeholder="Adress" value="<%= course.adress %>">
  </p>
  <p>
  How much would you like your course?
  <label for="inputCost"></label>
  <input type="text" name="cost" id="inputCost" placeholder="Cheaper is better" value="<%=course.cost%>">
  </p>
  <button type="submit">Submit</button>
  <br>
  <a href="<%=`/courses/${course._id}/delete?_method=DELETE`%>">DELETE this course.</a>
</form>